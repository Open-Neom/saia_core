name: saia_core_example
description: Demonstrates how to use saia_core for AI Spirit Ranks and Text-to-Speech configurations.
publish_to: 'none'

environment:
  sdk: ">=3.8.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  saia_core:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
