name: saia_core
description: Standalone AI core for the SAIA ecosystem. Zero dependency on neom_core.
version: 1.0.0
homepage: https://github.com/Open-Neom/saia_core
publish_to: 'none'

environment:
  sdk: ">=3.8.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
