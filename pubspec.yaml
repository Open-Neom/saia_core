name: saia_core
description: Standalone AI core models, use cases, and translation contracts for the SAIA ecosystem.
version: 1.0.0
homepage: https://github.com/Open-Neom/saia_core

environment:
  sdk: ">=3.8.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

